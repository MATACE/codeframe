#ifndef CODEFRAME_H
#define CODEFRAME_H

#include <stdio.h>

/// Glog path buf.
typedef struct {
    char glog_path[256];		///< Save glog log directory buf.
} SERVICE_PARAM_T;

#endif // CODEFRAME_H